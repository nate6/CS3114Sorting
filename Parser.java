import java.util.LinkedList;
import java.util.Scanner;

public class Parser {

    public static LinkedList<Record[]> parse(Scanner sc) {
        LinkedList<Record[]> blocks = new LinkedList<Record[]>();
        
        
        Record[] dfltArray = new Record[512];
        
        // TODO parse arrays
        
        
        blocks.add(dfltArray);
        
        return blocks;
    }

}
